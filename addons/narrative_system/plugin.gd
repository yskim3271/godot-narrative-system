@tool
extends EditorPlugin

# Editor integration entry point.
# Phase 7 adds: autoload registration in _enable_plugin()/_disable_plugin(),
# the bottom panel (database overview + validation), and project settings.
# The runtime under runtime/ must keep working without this plugin being enabled.
